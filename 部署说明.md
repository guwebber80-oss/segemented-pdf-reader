# 本地部署指南

> 正式版是**网页版**（`webapp/`），用法就是「本地跑一个小服务 + 浏览器打开」，
> 论文与缓存都留在你自己机器上。本文只讲这一条路。

---

## 一、日常使用（已经装好的机器）

**双击项目根目录的 `启动阅读器.bat`** 即可。它会依次：

1. 检查 `webapp\server.py`、虚拟环境、依赖与全部模块能否导入（缺依赖会给出修复命令并可一键安装）；
2. 顺手在后台把 GROBID 容器起起来（没装 Docker / Docker 没开会自动跳过，不影响阅读）；
3. 检查 8765 端口是否已被占用（已占用就直接帮你打开浏览器）；
4. 启动服务，并自动打开 <http://127.0.0.1:8765/>。

- **关闭那个黑窗口 = 停止阅读器**（或在窗口里按 `Ctrl + C`）。
- 只体检不启动：命令行执行 `启动阅读器.bat --check`。
- 停止 GROBID：另开窗口执行 `docker stop grobid`。
- 想用**旧界面（Streamlit 版）**对照：双击 `启动Streamlit版.bat`（8501）。两套前端共用同一份 `.cache/`。

---

## 二、换一台新电脑（5 步）

### 1. 装 Python

装 **Python 3.11 或更高**（3.14 已实测可用）。安装界面记得勾上 **Add Python to PATH**。

### 2. 拿到项目文件夹

整个文件夹拷过去即可，或者：

```
git clone https://github.com/guwebber80-oss/segemented-pdf-reader.git
```

### 3. 建虚拟环境并装依赖

在项目目录里开一个命令行（PowerShell 或 cmd），执行：

```
python -m venv .venv
.venv\Scripts\python.exe -m pip install -r requirements.txt
```

> `.venv` 在 `.gitignore` 里，**不会**跟着仓库走，所以每台新机器都要重建一次。
> 不用先 `activate`，直接用 `.venv\Scripts\python.exe` 调用即可（可绕开 PowerShell 的脚本执行策略限制）。

### 4. 配翻译 Key（可选）

在项目根目录新建 `.env` 文件（同样不在仓库里，需要自己建）：

```
TRANSLATOR_BACKEND=deepl
DEEPL_API_KEY=你的Key
```

- **没有 Key 也能用**：界面照常显示英文原文，中文按钮会提示"未配置后端"，其他功能不受影响。
- 备选后端：`openai` / `google`（写法见 `utils/translator.py` 顶部注释）。
- 也可以照 `.env.example` 复制一份再改。

### 5. 启动

双击 `启动阅读器.bat`。第一次可以先跑 `启动阅读器.bat --check` 只体检、不启动。

---

## 三、GROBID（可选，只影响「交叉校验」面板）

不装 Docker 完全不影响阅读、翻译、缓存——只是右栏少一个「GROBID 交叉校验」面板，
面板会如实显示"未检测到"并告诉你启动命令。

要用的话：

1. 装 **Docker Desktop**（Windows 需先启用 WSL2，装完要重启一次）；
2. 拉镜像（约 1.72GB，**不需要** 8GB 的全量版）：

   ```
   docker pull grobid/grobid:0.9.1-crf
   ```

3. 之后 `启动阅读器.bat` 会**自动拉起**它，不用手动敲命令。

**手动启动**（自动化失败时）：

```
docker run -d --rm --name grobid --init --ulimit core=0 -p 8070:8070 grobid/grobid:0.9.1-crf
```

自查：`docker ps` 里 grobid 那一行的 PORTS 必须显示 `0.0.0.0:8070->8070/tcp`。
**容器在跑 ≠ 端口通**——如果 PORTS 是空的，说明启动时漏了 `-p 8070:8070`，删掉容器重跑上面这条即可。

服务要十几秒才就绪；如果界面上还是"未检测到"，点右栏的「↻ 重新探测」。

---

## 四、数据在哪、怎么清

- 缓存目录：`项目根\.cache\`（已在 `.gitignore` 里，不进仓库）
  - `papers\<PDF 字节哈希>.json`：解析设置 + 人工修正的十项元数据 + GROBID 结果 + 读到第几张卡
  - `translations.json`：全局译文表（换论文可复用）
  - `index.json`：按 DOI / 标题兜底找回
  - **图片不落盘**，按需 200 DPI 重新渲染
- 清空方式：界面左栏「缓存管理」里的「清除本篇 / 清空全部」，或直接删掉 `.cache\` 文件夹。
- 想换盘：设环境变量 `PDF_READER_CACHE_DIR` 指向新目录。
- 论文文件本身**不会**被复制进缓存，缓存只是"读过的痕迹"。

---

## 五、常见问题

| 现象 | 原因与解法 |
|---|---|
| PowerShell 报「禁止运行脚本」 | Windows 默认策略，与本项目无关。直接用 `.venv\Scripts\python.exe -m webapp.server --port 8765`，或改用 cmd |
| 提示 8765 端口已被占用 | 已经有一个阅读器在跑。启动脚本会直接帮你打开浏览器；要重启请先关掉那个黑窗口 |
| 双击后浏览器没自动打开 | 手动访问 <http://127.0.0.1:8765/>（注意是 `127.0.0.1`，不要用 `localhost` 之外的地址） |
| 依赖或模块导入失败 | 执行 `.venv\Scripts\python.exe -m pip install -r requirements.txt`；仍失败请把报错发我 |
| 改了代码但界面没变化 | 浏览器按 **F5**（静态资源是 `no-store`，刷新即生效）；若改的是 `.py`，需要**关掉黑窗口重新双击启动** |
| 中文按钮提示"未配置后端" | 没写 `.env` 或 Key 无效；英文阅读不受影响 |
| 断网时某张卡没有译文 | 译文是"读到哪翻到哪"的懒加载。联网后点右栏「翻译整篇」，之后断网也能读中文 |

---

## 六、备份与迁移

要换电脑或做备份，拷这三样就够了：

1. **项目文件夹**（代码与文档，仓库里有）；
2. **`.env`**（你的 Key，仓库里没有，要单独拷）；
3. **`.cache\`**（读过的译文与元数据修正——不拷也能用，只是第一次要重新翻译）。
