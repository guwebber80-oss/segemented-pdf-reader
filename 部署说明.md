# 部署说明（阶段 7.3）

> 结论先说：**正式版是网页版（`webapp/`），用双击 `启动阅读器.bat` 在本地跑**。
> GitHub Pages **只能托管静态文件、跑不了 Python 后端**，所以它**不能**用来上线正式版；
> 它能做的是"在线看界面的静态演示"。下面把三条路和各自的代价写清楚。

---

## 一、为什么 GitHub Pages 上线不了正式版（事实）

本项目的架构是：

```
浏览器 ⇄ webapp/server.py（本地 HTTP 服务，Python）⇄ utils/（解析引擎）
             ↑                                    ↑
        必须在 Python 进程里跑               PyMuPDF 解析 PDF、写 .cache/
```

`GitHub Pages` 只提供**静态文件托管**（HTML / CSS / JS / 图片），**不执行任何服务端代码**，
也**没有文件系统**可写。正式版的核心——上传 PDF → PyMuPDF 解析 → 切卡片 → 调翻译 API → 落盘缓存——
全部发生在 Python 进程里，Pages 上无处安放。

另外两条硬约束：

1. **DeepL API Key 不能进前端**：Key 一旦写进 JS，任何人按 F12 就能拿走并刷你的额度。
   所以翻译必须由服务端发起——这也要求有后端。
2. **GROBID 是一个 Docker 容器**（8070），Pages 上更不可能跑。

> 换句话说：Pages 能托管的只有"壳"，而我们的能力都在"芯"里。

---

## 二、三条路的对比

| 方案 | 能做什么 | 代价 | 隐私 |
|---|---|---|---|
| **A. 本地部署（推荐，现在就能用）** | 全部功能：上传自己的 PDF、翻译、缓存、GROBID | 每台电脑装一次 Python 环境 | ✅ 论文不出本机（翻译只把**文本段落**发给 DeepL） |
| **B. GitHub Pages 静态演示** | 在线看界面与交互（**不能上传自己的论文**） | 需要预生成一份演示数据 | ✅ 无后端 |
| **C. 云端正式部署** | 任何设备打开链接就能用 | 要一台能跑 Python 的服务器（付费）；GROBID 基本只能关掉；需自建访问控制 | ⚠️ **论文会上传到第三方服务器** |

---

## 三、方案 A：本地部署（正式版，推荐）

### 已经在用的机器
双击 `启动阅读器.bat`（会自动拉起 GROBID 并打开浏览器 `http://127.0.0.1:8765/`）。
关掉那个黑窗口就是停止服务。

### 换一台新电脑（5 步）

1. 装 **Python 3.11+**（3.14 已实测可用），安装时勾选 *Add Python to PATH*；
2. 把本项目文件夹整个拷过去（或 `git clone https://github.com/guwebber80-oss/segemented-pdf-reader.git`）；
3. 在本项目目录里开个命令行，建虚拟环境并装依赖：
   ```
   python -m venv .venv
   .venv\Scripts\python.exe -m pip install -r requirements.txt
   ```
   > 注意：`.venv` 和 `.cache` 都被 gitignore，**不会**跟着仓库走，必须在新机器上重建。
4. 建一个 `.env` 文件（同样不在仓库里），写入翻译后端与 Key：
   ```
   TRANSLATOR_BACKEND=deepl
   DEEPL_API_KEY=你的Key
   ```
   没有 Key 也能用：界面照常显示英文原文，中文按钮会提示"未配置后端"。
   *（备选：`openai` / `google` 后端，见 `utils/translator.py` 顶部注释。）*
5. 双击 `启动阅读器.bat`。首次可先跑 `启动阅读器.bat --check` 只体检不启动。

**GROBID 是可选的**：不装 Docker 只是少一个「GROBID 交叉校验」面板，阅读、翻译、缓存全都照常。
要用就装 Docker Desktop，然后 `docker pull grobid/grobid:0.9.1-crf`（约 1.72GB，无需全量版）。

### 想换界面的话
`启动Streamlit版.bat` 是旧界面（端口 8501，对照用）。两套前端共用同一个 `.cache/`，
元数据修正、解析设置、译文都能互相看见。

---

## 四、方案 B：GitHub Pages 放"静态演示"（可选）

可行的做法是复用已有的 demo 工具链：

```
python .dsh-scratch/build_demo_data.py     # 用一篇论文生成自包含的单文件演示
→ segemented-pdf-reader/交互demo.html       # 约 0.9MB，数据与图片全内嵌
→ 拷到仓库的 docs/ 目录，GitHub 仓库设置里把 Pages 指到 docs/
→ 得到 https://<用户名>.github.io/segemented-pdf-reader/交互demo.html
```

**能演示**：三栏布局、卡片翻页、卡内滚动、沉浸模式、明暗模式、图片放大、中英切换（预翻译缓存）。
**不能演示**：上传自己的 PDF、实时翻译、缓存、元数据编辑。

> ⚠️ 演示文件里会内嵌**那一篇论文的正文与图片**。若该文献有版权顾虑或不宜公开，
> 请换一篇可公开的（如开放获取的 OA 论文）再发布，或干脆跳过方案 B。

---

## 五、方案 C：云端正式部署（需要时再说）

要真做成"打开链接就能用"，需要一台能常驻跑 Python 的机器。可选 Render / Railway / Fly.io / 自己的 VPS，
要点：

1. **启动命令**改成 `python -m webapp.server --port $PORT`，并让它**监听 0.0.0.0**
   （当前为了安全只监听 127.0.0.1，必须改成可配置，并且**必须加访问口令**，
   否则链接一旦泄露，任何人都能用你的 DeepL 额度、看你的缓存）；
2. **GROBID 关掉**（8GB/CPU 在云上不划算），交叉校验面板会自动进入"未检测到"的降级状态；
3. `.cache/` 要挂持久卷，否则每次重启都从零开始；
4. **隐私要自己权衡**：论文会被上传到第三方服务器。科研场景下，未发表稿件建议留在方案 A。

**我的建议**：现阶段用 **A（本地）**，需要给别人看界面时再用 **B（Pages 静态演示）**；
C 等真的有"多人/多设备"需求时再评估。
