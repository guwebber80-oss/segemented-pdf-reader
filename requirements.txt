# ============================================================
# 科研文献 PDF 智能阅读器 —— 依赖清单
# 安装命令（在项目根目录、已激活虚拟环境后执行）：
#     pip install -r requirements.txt
# ============================================================

# ---- Web 界面框架（前后端一体）----
streamlit>=1.40,<2.0

# ---- PDF 解析核心：提取文本块、字体、图片、链接 ----
PyMuPDF>=1.24

# ---- 读取 .env 中的 API Key（阶段 2 翻译功能会用到）----
python-dotenv>=1.0

# ---- HTTP 请求：调用翻译 API、GROBID 服务（阶段 2/4.3 会用到）----
requests>=2.31

# ---- XML/HTML 解析：解析 GROBID 返回的 TEI XML（阶段 4.3 会用到）----
lxml>=5.0
