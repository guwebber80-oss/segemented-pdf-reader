# ============================================================
# 环境变量模板
# 用法：把本文件复制一份，重命名为 .env，然后填入你自己的 Key。
#     Windows PowerShell:  Copy-Item .env.example .env
#     macOS / Linux:       cp .env.example .env
#
# ⚠️ .env 已被 .gitignore 排除，不会上传到 GitHub。
#    .env.example 只放「变量名」，永远不要放真实 Key。
# ============================================================

# ------------------------------------------------------------
# 翻译 API（阶段 2 使用）
# ------------------------------------------------------------
# 方案 A：DeepL（当前使用，科研文本质量最好）
#   注册地址：https://www.deepl.com/pro-api  → 选 "DeepL API Free"
#   免费额度：每月 1,000,000 字符（约 15~20 篇论文全文，实测确认）
#   免费版 Key 结尾是 ":fx"，代码会据此自动改用 api-free.deepl.com 域名
#   注册后在账号页面的 Account 一栏最下方可以看到 Authentication Key
DEEPL_API_KEY=

# 方案 B：OpenAI（备选，用 GPT 模型翻译，可自定义专业术语）
#   注册地址：https://platform.openai.com/api-keys
#   注意：按量付费，没有免费额度
OPENAI_API_KEY=
OPENAI_BASE_URL=https://api.openai.com/v1
OPENAI_MODEL=gpt-4o-mini

# 方案 C：Google Translate（备选，需要 Google Cloud 账号并开通 Cloud Translation API）
#   注册地址：https://console.cloud.google.com/apis/library/translate.googleapis.com
#   每月前 500,000 字符免费
GOOGLE_TRANSLATE_API_KEY=

# 默认使用哪个翻译后端：deepl / openai / google
# 界面上也可以随时切换（只列出配好了 Key 的后端）
TRANSLATOR_BACKEND=deepl

# ------------------------------------------------------------
# GROBID 服务地址（阶段 4.3 使用，本地 Docker 启动）
# ------------------------------------------------------------
GROBID_URL=http://localhost:8070
