"""
ui —— 界面层模块（阶段 5 从 app.py 拆出）

分法很保守：**只把自包含的辅助函数与面板搬出来**，脚本流程（侧边栏控件取值、
上传、解析缓存、卡片循环、诊断面板的调用点）仍留在 app.py。
原因是 Streamlit 的控件状态与「脚本每次重跑」的时序强耦合，
把流程本身搬进函数容易踩到会话状态与占位符时序的坑。

    ui.media   图片 / 公式 / 表格的区域截图与渲染（st.image 那类）
    ui.cards   阅读卡片：标签、中英切换、按段翻译与渲染

（后续若继续拆，下一步是 metadata_panel 与 diagnostics 两个大面板。）
"""
