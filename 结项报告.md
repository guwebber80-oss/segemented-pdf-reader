# 项目结项报告 · 科研文献 PDF 智能阅读器

> 结项日期：2026-09-16 ｜ 状态：**用户验收通过、结项**
> 仓库：https://github.com/guwebber80-oss/segemented-pdf-reader （private）
> 分支：`main` ｜ 结项 tag：`v1.0.0`

---

## 一、这个工具现在能做什么

上传一篇英文论文 PDF，它会：

1. **解析**为「正文卡片」——100~300 词一张、语义连贯、带章节标题与页码，像 PPT 一样单卡翻页（左右 `‹ ›` 或方向键）；
2. **图片与公式/表格按原排版截图**，跟在对应卡片后面，可点击放大；
3. **中英一键切换**（默认中文），**引文不翻译**，公式与表格以图呈现、不送翻译；
4. **元数据自动提取且可人工修正**：标题 / DOI / 摘要 / 作者 / 第一作者 / 通讯作者（信封图标标记）/ 单位 / 附件链接，
   每个字段标注来源、可编辑、可一键还原；
5. **GROBID 交叉校验**作「第二意见」：并排对照、逐字段一键采信；
6. **本地持久缓存**：译文与修正落到磁盘，关掉重开、断网也能接着读中文（含「译文覆盖度」与「翻译整篇」）；
7. **解析诊断**：块/段落/卡片统计、字号基准、公式与表格区域清单、插图清单与关联、跳过原因、每页排版；
8. **解析设置**：段落合并 / 行尾连字符 / 卡片目标词数 / 表格呈现 / 显示全部（核对模式）/ **小标题补丁**；
9. **界面**：三栏（左=功能与导航、中=卡片、右=元数据与背景信息）、**固定页面 + 卡内滚动**、
   **沉浸模式**（`I` 键，隐藏左右栏）、明暗模式、快捷键 `← → / L / I / Esc`。

## 二、交付物

| 类别 | 内容 |
|---|---|
| 正式版 | `webapp/`（自建前端，端口 8765），入口 **`启动阅读器.bat`** |
| 对照版 | `app.py` + `ui/`（Streamlit，端口 8501），入口 `启动Streamlit版.bat` |
| 引擎 | `utils/`（10+ 模块，**零 Streamlit 依赖**，有架构守卫测试强制） |
| 文档 | `README.md`（含各阶段设计决策与踩坑）、`部署说明.md`（本地部署）、`GROBID对比结论.md`、`核对指南.md`、`卡片一屏适配-可行性分析.md` |
| 工具 | `启动阅读器.bat` / `启动Streamlit版.bat` / `回归检查.bat` / `导出对话记录.bat` |
| 测试 | `tests/` 12 个仓库套件（281 条断言）+ `.dsh-scratch/` 开发侧套件 + 10 篇样本全样本回归基线 |

## 三、质量证据（结项时实测）

- **全样本回归**：10 篇样本（6 个出版社）与基线**逐字段一致**，判定「原有功能未受影响」
- **测试**：12 个仓库套件 281 条断言全绿；`webapi_roundtrip` 74 条 HTTP 端到端、`webui_js_test` 29 条前端事件全绿
- **版本纪律**：每个阶段一个提交、每步可单独 `git revert`；两次失败尝试都有 tag 留档并已回滚验证
  （`v0.6.1-layout-broken` = 失败留档、`v0.6.0` = 回滚安全点）

## 四、明确不做 / 已知边界（交接必读）

**能力边界**
- **扫描版 PDF 不支持**（无 OCR）——最大的一条边界；
- 表格**不跨页合并**、不转 Markdown（刻意：列关系一旦线性化就丢失）；
- 公式**不转 LaTeX**（刻意：OCR 出来的公式「看起来很对」，比看原图更危险）；
- 引文不翻译（按需求）；
- 通用性只在 **10 篇样本 / 6 个出版社**上验证过，新排版可能需要调判据（判据参数都集中在模块顶部，便于按样本调）。

**用户已决定不做**
- 领域术语表（因此 DeepL 把小鼠研究的 males/females 译成「男性/女性」的问题仍然存在）；
- GROBID 全量镜像（8GB）——实测 CRF 版已够且与我们互补；
- 在线托管（GitHub Pages 只托管静态文件、跑不了 Python 后端，且 Key 不能进前端）；
- 元数据「从 PDF 重新提取」入口、「导入后自动翻译整篇」开关。

**一个已知小遗留**
- Cell Press 那篇有 2 个字母/数字短块由「栏目名/标签」落进正文（多 8 词，基线已按此刷新）。
  要精确治它需要一条更窄的规则（如「零数字 + ≤3 token」），本轮试过的宽规则会伤正文，已回退。

## 五、这段协作的经历（可直接用作总结素材）

**一句话**：一个「外行提需求 + AI 写代码」的 vibe coding 项目，靠**分阶段、可验证、可回滚**这三条铁律，
从 0 走到可用；两次界面失败都靠版本控制与回滚活了下来，并最终促成了一次架构转向。

**阶段推进的节奏**：用户定下「一次只做一个阶段，完成→验收→提交 GitHub→再进入下一个」，
于是项目被切成 0→7.3 共十余个阶段，每阶段都配一份「点哪里、应看到什么」的验收清单。
用户验收反馈极简（「验收通过」四个字），所以**自测、编号清单、预期数字全部由 AI 主动补齐**。

**两次失败与一次转向**（最有价值的一段）：
1. 需求「固定页面 + 卡内滚动 + 沉浸模式」先做了单文件 demo，用户在 demo 上验收通过；
2. 照搬到 Streamlit 上，第 1 版押「注入 CSS + 命中框架类名」→ 用户实测**完全没生效**；
3. 第 2 版改用「压列宽 + 1px 容器藏起来」的 hack → 用户实测**界面完全错乱**；
4. 全部 `git revert` 回安全点、用 `git diff` 验证零差异；
5. 用户的追问（「记忆还在不在 / 有没有版本控制 / demo 上成立为什么这里失效」）逼出了关键判断：
   **框架的能力边界挡住需求时，「换前端」比继续搏斗更省事**——前提是引擎层不依赖框架。
   于是自建网页版（`webapp/`）落地，同一批需求**一天内**就按 demo 的效果成立了。

**沉淀下来的几条原则**
- 能用官方 API 就用官方 API，不赌「自己无法验证」的链路（沙箱里没有浏览器，CSS 是否生效不可自证）；
- 一次只改一件事，否则失败后连「哪一半坏的」都分不清；
- 判据类改动必须带**回退开关**，并用「关掉开关 = 与改动前逐字段一致」来证明安全性；
- 提取类结果一律是**候选值**，界面上必须可改、必须标来源、没有证据就如实报「未找到」，绝不猜；
- 凡是「没显示全」的地方都要显式化（译文覆盖度、卡内滚动提示、GROBID 未检测到的原因）；
- 失败后先给**可回滚的安全点与证据**，再解释根因，最后才谈修复。

## 六、结项后怎么用

双击 **`启动阅读器.bat`** 即可。换电脑、配翻译 Key、装 GROBID、清缓存、常见问题见 `部署说明.md`。
想回顾这段协作的完整对话，双击 **`导出对话记录.bat`**（见 `导出对话记录.py` 顶部说明）。
