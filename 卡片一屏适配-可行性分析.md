# 卡片「一屏适配」可行性分析（阶段 6.3 备选，本次不落地）

> 需求原话（用户 2026-09-15）：**「卡片文本+图片长度限制在当前页面能显示的长度」**
> 状态：**只做可行性分析，先不写代码。** 等你看完这份判断再决定做不做、做哪一档。

---

## 一、现状为什么不满足

1. **卡片是按「词数」切的，和高度无关**。目标是 100~400 词（实际落点 0.5~1.5 倍），
   实测 npj 那篇最长的一张卡 332 词——词数相同的两张卡，高度可以差一倍（小节标题多、
   行短、段落多都会变高）。
2. **插图按宽度铺满**（920px 的图列），高度完全由**图片自己的宽高比**决定：
   一张 3:4 的竖版图渲染出来 ≈ 1200px 高，一张 6 列表格截图 400~600px。
   也就是说「图有多高」这件事现在**完全没人管**。
3. **一张卡可以带多张插图**（实测同一张卡最多 3 张），叠加起来轻松超过两屏。
4. **中文视图的行数与英文不一样**：中文每行字符数约为拉丁文本的一半（≈45 vs ≈95），
   同一张卡切到中文可能比英文更长或更短——「一屏」这件事必须两种语言都成立。

粗略预算：笔记本视口高 768~900px，减掉顶部标题、卡片标签、进度条、上下边距 ≈ 200px，
**一张卡真正能用的高度只有 500~650px**。

## 二、四个决定方案边界的实测事实

| 事实 | 结论 |
| --- | --- |
| `st.container(height=N)` **存在**（1.63，签名已核对） | 可以给卡片主体一个**固定高度 + 内部滚动**的区域，外层页面永不滚动 |
| `st.image` **没有 height 参数**（只有 `width`） | 想给图片限高，只能按宽高比**反算显示宽度**：`width = min(列宽, 限高 × w/h)`；宽高比在 `extract_images` 里现成（图片自身 w/h），**可算** |
| Streamlit **不暴露视口高度**（`st.context` 里没有） | 要么让用户选一个档位，要么自建组件测量（见方案 C） |
| 切卡发生在解析层 `utils/pdf_parser.py`，且被 10 个样本的基线指纹锁着 | 改切卡判据 = **基线全变**，需要重采基线 + 逐样本人工复核 |

## 三、三个方案

### 方案 A：纯界面层（**推荐先做**）

* 卡片主体放进 `st.container(height=N)`：**页面永远一屏**，翻页按钮位置固定；
  长内容在卡片内部滚动。
* 插图 / 公式截图 / 表格截图按「限高」反算显示宽度（用现成的宽高比），
  超出部分点 **🔍 放大**看原尺寸（弹层已有，不用新做）。
* 左栏加一个「卡片高度」档位（560 / 620 / 680 / 自动），存在本地缓存里记住。

| 项 | 说明 |
| --- | --- |
| 成本 | 约半天 |
| 风险 | 低：**完全不动解析逻辑**，10 个样本基线不变，随时可回退 |
| 效果 | 页面永不滚动、翻页按钮固定、图片不会再撑爆屏幕 |
| 代价 | 长卡片要**在卡内滚动**（不是「完全不滚」）；嵌套滚动条需要在视觉上做出区分（边框/渐变提示） |

### 方案 B：解析层按「视觉行数」切卡

把切卡判据从「词数」换成「估算渲染行数」：拉丁文本 ≈95 字符/行（760px 列，16px 字号），
中文 ≈45 字符/行，取 **EN / ZH 两者较大值**，保证每张卡 ≤ 14~16 行。

| 项 | 说明 |
| --- | --- |
| 成本 | 1~2 天（含 10 个样本重新验收、重采基线） |
| 风险 | 中：切卡质量直接决定阅读体验，必须逐样本人工看（"两张卡被切成三张"也是体验下降） |
| 效果 | 卡片长度更均匀，长卡片从根本上变少 |
| 限制 | **管不住图片高度**（图高由宽高比决定）→ 必须和方案 A 配合 |

### 方案 C：测量真实视口高度（自适应）

自建一个 Streamlit 组件（iframe + JS 把 `window.innerHeight` 报回来）来自动决定 N。

| 项 | 说明 |
| --- | --- |
| 成本 | 约 1 天 |
| 风险 | 偏高：依赖自建组件的沙箱/CSP 行为，Streamlit 升级可能失效 |
| 替代 | 方案 A 里的**手动档位**已经能解决 90% 的问题，且零依赖 |

## 四、建议

1. **先做方案 A**（半天、不动解析、基线不变、可随时回退）。它解决的是最影响体验的那一半：
   翻页按钮位置固定 + 图片不再撑爆屏幕。
2. 观察一两天：如果「长卡片」仍然频繁（比如 p9 那种**标题与首条参考文献并块**的已知缺陷
   造出的 332 词卡），再考虑方案 B 的切卡判据改造（那时需要重采基线并逐样本验收）。
3. 方案 C 暂不考虑——收益（自动档位）与脆弱性不成比例。

## 五、一句实话

**「一屏一卡 + 完全不滚动」对论文是不现实的**：400 词的段落 + 3 张图在 13 寸到 27 寸的
任何屏幕上都放不下。现实、也可验收的目标是：

> **页面永不滚动（翻页按钮固定不动）+ 卡片内部可滚动 + 图片按高度自适应缩放（点开放大看原尺寸）**

如果你要的是字面意义的「一个字都不滚」，那就只能靠方案 B 把卡片切得很碎
（一屏卡可能只剩 6~8 行正文），阅读连贯性会明显下降——这个取舍需要你来定。
